[package]
name = "fat"
description = "Byte-accurate FAT filesystem emulators on an in-memory disk: FAT16 today, FAT32 designed in"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
fs-core = { path = "../fs-core" }
